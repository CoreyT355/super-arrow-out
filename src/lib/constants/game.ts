// Game rules constants. Anything that changes the rules of play lives
// here so the change shows up in one PR.

/** How many wrong taps a player gets before the game ends. */
export const MAX_LIVES = 3;
